import type { ComponentType, HTMLAttributes } from 'react';

// Contract-only component type for {{pascalCase name}}. Module/consumer UI imports
// this type; every UI provider (daisyui, MUI, ...) implements it. No UI library
// is imported here.
export interface {{pascalCase name}}Props extends HTMLAttributes<HTMLDivElement> {
  // AGENT: define props
}

export type {{pascalCase name}}Component = ComponentType<{{pascalCase name}}Props>;
