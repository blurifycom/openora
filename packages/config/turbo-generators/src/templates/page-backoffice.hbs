import { createFileRoute } from '@tanstack/react-router';
import { {{component}} } from '@oss/react-pages';

export const Route = createFileRoute('/_authed/{{route}}')({
  component: {{component}},
});
