import { {{component}} } from '@oss/react-pages';

// Route shim. The page body lives in @oss/react-pages; this file just mounts it.
export const dynamic = 'force-dynamic';

export default function Page() {
  return <{{component}} />;
}
