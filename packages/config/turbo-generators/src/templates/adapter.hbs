import { definePlugin } from '@oss/plugin-host';
import { {{token}}, type {{pascalCase token}} } from '@oss/adapters';

// Overlay that swaps the {{token}} binding. Registered AFTER `{{dependsOn}}` (which
// owns the default binding) so this one wins - last registration of a DI token wins.
// Implement every method of {{pascalCase token}} against your vendor. The factory's
// declared return type keeps you honest at compile time; it throws until implemented.
function create{{pascalCase name}}Adapter(): {{pascalCase token}} {
  // AGENT: implement the {{pascalCase token}} interface here, then remove the throw.
  throw new Error('{{kebabCase name}} adapter is not implemented yet');
}

export default definePlugin({
  id: '{{kebabCase name}}',
  dependsOn: ['{{dependsOn}}'],
  register(ctx) {
    ctx.provide({{token}}, () => create{{pascalCase name}}Adapter());
  },
});
