// Adapter interfaces (ports) for the lobby module.
// The lobby module has no external third-party integrations.
// Implement adapters in adapters/<vendor>/ and inject via Nest DI if needed in future.
