import * as z from 'zod';

// Operator-only config block for {{pascalCase name}} (feature flags, brand/RG defaults,
// etc). To activate it:
//   1. Merge {{pascalCase name}}ConfigSchema into PlatformConfigSchema
//      (packages/contracts/shared-schemas/src/platform-config.ts) via `.extend(...)`.
//   2. Read it through `loadPlatformConfig()` (@openora/core), bound on the
//      PLATFORM_CONFIG container token.
// No admin UI in v1 - operators edit platform-config.yaml/.json. See ADR-0013 T0.
export const {{pascalCase name}}ConfigSchema = z.object({
  enabled: z.boolean().default(false),
  // AGENT: define operator-tunable fields here
});

export type {{pascalCase name}}Config = z.infer<typeof {{pascalCase name}}ConfigSchema>;
