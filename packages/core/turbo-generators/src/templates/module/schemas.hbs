// Module-local Zod schemas for {{pascalCase name}}.
//
// The canonical request/response shapes live in the oRPC contract slice
// (packages/contracts/orpc-contract/src/{{kebabCase name}}.ts) - re-export from there
// rather than redefining. Promote anything shared across modules to
// @blurifycom/shared-schemas. Never define ad-hoc Zod inside router/ (lint: no-adhoc-zod-in-router).
//
// Example:
// export { {{pascalCase name}}Schema } from '@blurifycom/orpc-contract/{{kebabCase name}}';
export {};
