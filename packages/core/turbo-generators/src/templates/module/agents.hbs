# {{kebabCase name}}

<!-- One paragraph: what business domain this module owns and the one design decision a reader
     must know. Point at where facts live instead of re-listing them - routes and schemas are in
     `contract/` and `schema/index.ts` (or `list-routes module={{kebabCase name}}`), events in
     `domainEventSchemas`. -->

This file holds ONLY what code can't say: invariants, rationale, gotchas, extension seams,
where-to-look pointers. Never route/table/layout/event listings - they duplicate `contract/`,
`schema/`, `docs/catalog.json` and drift.

## Invariants

<!-- Money/authz/idempotency/ordering rules the code enforces but a reader could miss:
     fail-closed branches, DB guards, race windows accepted by design, "X is the single
     writer of Y". Delete the section if the module honestly has none yet. -->

## Extension points

<!-- Ports provided/consumed (token + one line on why), events others may react to,
     what an overlay can rebind and where the default binding lives. -->

## Don't

<!-- Module-specific bans only - global rules (cross-module imports, float money,
     inline Zod in routers) are lint-enforced already; don't repeat them. -->
