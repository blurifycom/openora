// Public surface of the {{pascalCase name}} add-on. Export only what other packages
// may consume; internal implementation details stay private. Cross-module table
// reads go through the `@openora-addons/{{kebabCase name}}/schema` subpath.
export { {{pascalCase name}}Service } from './service/{{kebabCase name}}.service.js';
export { create{{pascalCase name}}Router } from './router/index.js';
