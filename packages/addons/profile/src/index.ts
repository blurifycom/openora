// Public surface of the Profile module: the player-facing self-profile.
// Owns the `player` table (read by others via the /schema subpath).
export { ProfileService } from './service/profile.service.js';
