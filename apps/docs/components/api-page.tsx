'use client';
import { createOpenAPIPage } from 'fumadocs-openapi/ui';

// Rendered by the MDX files generated from docs/openapi.json (scripts/generate-openapi.ts),
// which pass `document` + `operations` props themselves.
export const OpenAPIPage = createOpenAPIPage();
