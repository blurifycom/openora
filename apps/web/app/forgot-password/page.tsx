import { ForgotPasswordPage } from '@oss/react-pages';

// Client-driven form (TanStack Query mutation); render dynamically.
export const dynamic = 'force-dynamic';

export default function Page() {
  return <ForgotPasswordPage />;
}
