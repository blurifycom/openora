import { z } from 'zod';

// Module-local schemas for {{Name}}. Re-export from @oss/contracts/domain-schemas
// for anything shared across modules.
//
// Example:
// export { UserSchema } from '@oss/contracts/domain-schemas';
// export const {{Name}}CreateSchema = z.object({ ... });
