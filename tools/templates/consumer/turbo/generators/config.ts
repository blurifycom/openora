// Generators live in the platform (@oss/turbo-generators) so they stay in sync with
// the contracts they scaffold. `pnpm gen plugin|adapter|page` resolves through here.
// To customize, replace this re-export with your own plop config.
export { default } from '@oss/turbo-generators';
