import { {{component}} } from '@oss/react-sdk';

// Route shim. The page body lives in @oss/react-sdk; this file just mounts it.
export const dynamic = 'force-dynamic';

export default function Page() {
  return <{{component}} />;
}
