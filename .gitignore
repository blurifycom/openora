.DS_Store
node_modules
dist
.next
.turbo
coverage
*.local
.env
.env.*
!.env.example
prisma/migrations/*.sql
*.tsbuildinfo
# stray OpenAPI spec emitted when the API boots with cwd=apps/api (the canonical
# one is docs/openapi.json at the repo root, read by gen-catalog)
apps/*/docs/openapi.json
.cache
storybook-static
.claude/worktrees
# TanStack Router file-based route tree (generated by tsr generate / vite plugin)
routeTree.gen.ts
# TanStack Router plugin cache
.tanstack

# Generated by rulesync from .rulesync/ (single source of truth).
# Regenerated on `pnpm install` (prepare) or `pnpm sync:agents`. Do not commit.
# Root-anchored so per-package AGENTS.md (hand-written) stay tracked.
/AGENTS.md
/CLAUDE.md
/GEMINI.md
/.mcp.json
.vscode/mcp.json
.gemini/
.claude/agents/
.claude/commands/
.github/copilot-instructions.md
.github/agents/
.github/prompts/
.github/hooks/
.github/instructions/
.claude/rules/
.agents/
